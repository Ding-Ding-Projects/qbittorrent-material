# Roadmap

## Completed

- 2026-07-30: added persisted English, playful Hong Kong-style Cantonese, and
  bilingual modes with independent funny-level controls; a persistent
  notification center; the offline startup dim-sum surprise; the complete
  in-app changelog; searchable quick Settings; external-editor launch; and
  always-on settings history.
- 2026-07-30: upgraded the personal Workspace to schema 2 with a stable pinned
  region, groups, overflow, four independent tab-discovery searches, bounded
  Qt/PCRE2 construction, reviewed containing/inverse bulk close, sparse
  global/group/tab appearance, continuous color translation, and portable
  named presets.
- 2026-07-30: hardened the Windows release contract with hosted-runner capacity
  measurement, a desktop-only policy test, immutable monotonic tags, the
  required token fallback, and a real installer plus bundled dim-sum image on
  each successful push or dispatch.
- 2026-07-21: hardened optional workspace navigation, repaired the application
  menu's plugin-management dead end, and made the Split Dock details tabs
  focusable buttons. Verified with a clean build and the complete 17-state
  native capture sweep.

## Follow-up

- Capture fresh installed-build evidence for the new settings, notifications,
  changelog, dim-sum, tab-search, grouping, and appearance surfaces after the
  integrated build is green.
- Extend repeatable Windows UI automation to pointer reorder, pinned protection,
  collapsed-group result activation, both bulk-close predicates, editor anchor
  collision, keyboard focus return, and preset file dialogs.
- Revisit Qt rendering support for double strikethrough, custom underline,
  outline/glow, arbitrary variable-font axes, and recursively previewing every
  unfinished target override on the editor's own chrome. Persisted metadata and
  the current capability explanations must remain intact until rendering grows.
